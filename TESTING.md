# 实测指南（浏览器）

## 一、启动

```powershell
cd "E:\WorkFolder\DSH Desktop\word-chain"
node server.js
```

Windows 也可以直接双击 `启动游戏.bat`。

控制台出现这三行就对了：

```
单词接龙(服务端) 已启动: http://localhost:8080
词库词条: 281889（含知识点: 13599）
词库来源: 全量词库 data/db.json
```

浏览器打开 **http://localhost:8080** → 点「以游客身份继续」→ 选模式。
（改过代码后按 **Ctrl+F5** 强制刷新）

> 换端口：`$env:PORT=9000; node server.js`

---

## 二、测「词库清理」（任何模式都能测）

出词 → 系统校验 → 玩家出词时**词在不在词库**这件事与模式无关（删掉的词在哪儿都用不了）。

1. 模式随便选（人机对战最快），点「开始」
2. 出 `abarticular`（这是个正常词，能开局）
3. 轮到对方（或 AI）时，输入 `lar`
   → **应弹出「lar 不在词库中，不计入难度统计，请确认是否使用」** ✅
   （`lar` 已被词库清理删除；同理可试 `te` / `ogy` / `sis` / `zed`）
4. 对照：输入 `oat`（真词，有独立意思）→ **在库，直接通过** ✅

---

## 三、测「禁止回声」⚠️ **只有联机房间有**

> 人机对战、本地同屏、离线便携版都**允许回声**（刻意如此：这条规则只用于人与人对战）。

先按第四节建好双窗口房间，然后：

| 甲出 | 乙出 | 预期 |
|---|---|---|
| `envisage` | `age` | **通过** ✓ 高频词允许回声 |
| `bloat` | `oat` | **被拒** ✓ 提示「不能直接把上词的结尾「oat」当作你要出的词」 |
| `boa` | `oat` | **通过** ✓ 不是回声，是正常接龙 |

每换一组，甲方点「终止对局」回等待区再重新开局即可。

---

## 四、测「联机房间」（真实双人，同一台机器也行）

两个登录态需要两个浏览器环境：

1. **普通窗口**打开 http://localhost:8080 → 注册并登录账号 `甲`
2. **无痕/隐身窗口**打开同一地址 → 注册并登录账号 `乙`
   （普通窗口与无痕窗口的登录态互不影响，所以能同时在线）
3. 甲：模式选「联机房间」→「创建房间」→ 记下 **6 位房间号**
4. 乙：「加入房间」→ 输入房间号
5. 甲：「开始对战」
6. 甲出 `envisage` → 乙出 `age` → **应通过**
7. 甲点「终止对局」回到等待 → 重新开始 → 甲出 `abarticular` → 乙出 `lar` → **应弹确认**

顺便可以试的联机功能：轮到谁会有倒计时（60 秒不出词自动认输）、房主才能开始/终止/解散、玩家列表里可以点「单挑」。

---

## 五、一条命令看全景（不用开游戏）

```powershell
node tools/check_echo.js
```

输出：词库规模、6 组「上词 → 回答词」在游戏中的实际判定、以及"该删的删了、该留的留了"是否全部符合预期。

**想自己试别的词**：编辑 `tools/check_echo.js` 顶部的两个数组即可

```js
var TEST_PAIRS  = [['上词', '回答词'], ...];   // 试回声判定
var CHECK_WORDS = ['lar', 'oat', ...];        // 查在不在词库
```

---

## 六、测「AI 裁判验词」⚠️ **只有联机房间有**

> 人机对战和本地同屏**不弹验词**（刻意如此）。

用第三节的双窗口房间来测：

1. 甲（房主）点「开始对战」
2. 甲输入 `aardvark` 出词（极生僻词，必定触发）
   → 甲那边**立即弹出验词窗口**，问「aardvark」是什么意思，4 个选项（3 个释义 + 「以上都不对」），
   并显示 **「⏱ 剩余 N 秒」**
3. 看乙那边：**看不到题目**（题目只发给该作答的人），只显示"xxx 接龙中…"
4. **故意不答，等 15 秒** → 甲被扣 1 分，提示「甲 验词超时未作答，扣 1 分，由下一位继续」，**乙开始接龙**
5. 重开一局，这次甲选**错** → 提示「答错了，扣 1 分；正确答案是…」，乙继续接龙
6. 再重开，这次选**对** → **不扣分**，乙继续接龙

**判定口径**（想调松紧只改 `src/ai.js` 的 `CFG` 与权重 `W`）：

| 情形 | 是否验 |
|---|---|
| 极生僻词（无佐证且常见度 < 0.30） | **必验**（不管快慢） |
| 生僻词（f < 0.45）+ 秒回（≤1.2 秒） | 验 |
| 常用词（如 apple）+ 秒回 | **不验** |
| 难度比你自己此前高出很多 / 超出你的学习画像 | 累积可疑度，够了就验 |

同一玩家 60 秒内最多被验一次（`COOLDOWN_MS`）；待作答时不能继续出词。

> 注意：有 25% 概率**正确释义不在选项里**，此时「以上都不对」才是答案（防止瞎猜）。
> 想改成"正确答案永远在选项里"：把 `src/ai.js` 的 `CFG.NONE_OPTION_RATE` 调成 `0`。

---

## 七、跑测试（改代码后必做）

```powershell
npm.cmd test
```

> ⚠️ PowerShell 里必须用 **`npm.cmd`**，直接敲 `npm` 会被执行策略挡住（`npm.ps1` 无法加载）。

预期：7 套全绿。若只想看结论：

```powershell
npm.cmd test > $null 2>&1; $LASTEXITCODE   # 0 = 全部通过
```

> 不要用 `npm.cmd test 2>&1 | Select-String ...` 来判断成败 —— Node 的弃用警告会被当成错误，
> 让退出码显示为 1（实际测试是全绿的）。
