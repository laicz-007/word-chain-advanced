# WORKFLOW.md — 项目维护与演进工作流

> 这份文档解决一个问题：**这个项目为什么容易越改越乱，以及每一步该怎么走才不会乱。**
> 所有结论都经过实际验证（命令、数字、行号可复查），不是泛泛的建议。
> 建议每次开工前先看第 4 节，改完对照第 5 节的验收关卡。

---

## 1. 项目概况（30 秒看懂）

### 1.1 三种交付形态

| 形态 | 入口 | 依赖 | 数据存哪 |
|---|---|---|---|
| **网站版** | `server.js` | 只需 Node.js（运行时零第三方依赖） | 服务端 `data/`，按账户 |
| **便携版** | `word-chain-standalone.html`（单文件 11MB） | 浏览器，零安装 | 浏览器 localStorage |
| **词库工具链** | `npm run build` | 额外需要 **Python 3** | 产出 `data/db.json` |

### 1.2 代码构成

| 语言 | 文件数 | 行数 | 占比 | 位置 |
|---|---|---|---|---|
| JavaScript | 24 | 3,905 | 87.6% | 运行时 / 前端 / 测试 / 大部分工具 |
| Python | 3 | 552 | 12.4% | **只在 `tools/`**，纯离线词库构建 |

**关键事实**：Python **完全不碰游戏规则**。三条规则（末尾 2/3 字母匹配、元音结尾、禁 `ry/ht/ck`）只存在于 `public/logic.js` 一处。Python 只做词库整理（合并释义、推导难度、提取知识点）。

### 1.3 当前基线（改动前请先记录，改动后用来对比）

| 指标 | 当前值 |
|---|---|
| 全量词库 `data/db.json` | **282,551** 词（83.67 MB） |
| 轻量词库 `data/db.lite.json` | **36,809** 词（11.86 MB） |
| 基础词库 `public/vocab.json` | **28,531** 词 |
| 测试 | 7 套全部通过（其中 6 套带计数，共 **124** 项检查） |
| 全量库 kind 分布 | 0.9→282,539 / 0.05→10 / 0.1→2 |

---

## 2. 系统联系图（谁依赖谁）

### 2.1 模块依赖（`require` 关系，实测生成）

```
server.js ──┬─> src/config.js
            ├─> src/db.js ──────> public/logic.js   ★ 共用规则引擎
            ├─> src/auth.js ────> src/config.js
            ├─> src/userdata.js ─> src/config.js, src/db.js
            ├─> src/usage.js ───> src/config.js, src/userdata.js
            ├─> src/view.js ────> src/db.js
            ├─> src/gameplay.js ─> src/db.js, src/view.js
            ├─> src/rooms.js ───> src/db.js, src/view.js, src/gameplay.js
            └─> src/api.js ─────> 上面全部 8 个模块

tools/compute_chain_idx.js ─> public/logic.js   ★ 同一份引擎
tools/analyze_hard_ends.js ─> public/logic.js   ★ 同一份引擎
tools/build_standalone.js  ─> （把 logic.js/app.js 内联进 HTML）
```

**★ 最重要的一条联系**：`public/logic.js` 被**服务端、浏览器、词库构建工具三方共用**。
它是唯一的规则真相源 —— 好处是规则绝不会前后端不一致；代价是**改它必须同时验证三边**。

### 2.2 数据流（源码 → 产物）

```
data/word.csv + word_translation.csv
        │  python tools/build_data.py
        ▼
   public/vocab.json ──────────────┐  （28,531 词，基础池）
                                    │  python tools/build_unified_db.py
   data/ecdict.csv ────────────────┤  （联网下载 ECDICT/Tofu/Kyle）
   data/tofu_words.csv ────────────┤
   data/kyle/*.jsonl ──────────────┘
                                    ▼
                        data/db.json（约 33 万词）
                                    │  node tools/compute_chain_idx.js
                                    │  （算可接指数 + 过滤缩写/专名）
                                    ▼
                        data/db.json（282,551 词）★ 服务端优先使用
                                    │
   data/db.lite.json ──────────────┴──> src/db.js（兜底）+ build_standalone.js
   （36,809 词，随仓库分发，克隆即可玩）

tools/analyze_hard_ends.js ─> tools/hard_ends_report.json ─> tools/print_hard_list.js
```

### 2.3 三条关键链路

**链路 A — 一次出词请求**
`浏览器 app.js` → `POST /api/action` → `src/api.js` → `src/gameplay.js` → `logic.js Game.submitChain` → `src/view.js snapshot` → 浏览器渲染

**链路 B — AI 选词**（核心算法）
`gameplay.advanceAI` → `logic.js Game.tickAI` → `store.candidates()`（用 `by2`/`by3` 前缀索引）→ `scorePick()` 四维加权评分 → `submitChain`

```
score = 硬门禁(合规+非死路) × (0.30·防疲劳 + 0.23·难度贴合 + 0.23·词频 + 0.24·可接指数)
        × 词型惩罚 × 结尾多样化系数 ×1.18(教学) × S5[0.93,1.07]随机
```

**链路 C — 词库构建**（见 2.2，注意它跨两种语言）

---

## 3. 易碎点清单（"难维护"的根因）

按危险程度排序。**每一条都有实证**。

| # | 风险 | 具体位置 | 后果 | 对策 |
|---|---|---|---|---|
| 1 | **过滤正则与数据格式脱节** | `compute_chain_idx.js:126` 锚定 `/^\[网络\]/`，但 283 个词里 `[网络]` **全在释义中间**，开头 0 个 | 规则看似存在，**实际删 0 个词**（空转） | 改过滤规则后**必须跑一次"实际删了多少"的验证**，不能只看代码 |
| 2 | ~~**专名识别基本失效**~~ ✅ **已修复** | 原 `compute_chain_idx.js:31-32` 要求 `[人名]` 或 `(国名…)`，但 ECDICT 真实写法是 `(Aage)人名；(丹)奥格`（"人名"在括号外） | 修复前：全量库 **7,056** 个专名注记词 **100% 漏网**，全被判为普通词（kind=0.9）→ 已改为 kind=0.10。**教训**：不能用宽泛的 `/人名\|地名\|姓氏/` —— `warehouse` 的释义含"以他人名义"，子串巧合会误伤真常用词；必须匹配"括号注记"上下文 |
| 3 | **可接指数被 2 字母回退顶满** | 规则允许"末尾 2 字母"匹配，`ar`/`er`/`le` 等高频结尾导致 `chain_idx≈0.9` | 946 个 `-lar` 结尾的**生僻医学形容词**全部拿到满分可接指数，AI 主动喂给玩家（如 `acromioclavicular`=肩锁骨的） | 改 `compute_chain_idx.js` 的供给口径，或对"高可接+无权威佐证"组合降权 |
| 4 | **生成物被当成源码入库** | `public/vocab.json`、`data/db.lite.json`、`tools/hard_ends_report.json` 都被 git 跟踪 | 会**静默漂移**：`hard_ends_report.json` 已是 8/31 基于 36,809 词的快照，早已过期 | 跟踪的生成物必须写明"由谁生成、何时生成" |
| 5 | ~~**工具链断链**~~ ✅ **已修复** | `print_hard_list.js` 读 `rep.words`/`rep.summary`，实际字段是 `rep.rows`/`rep.meta` | 运行**立即崩溃**（`TypeError: Cannot read properties of undefined`） | 已按真实结构重写，并加了"报告结构不对就给人话提示"的自检；消费方做字段校验是关键 |
| 6 | **双份前端代码** | `word-chain-standalone.html` 内嵌 `logic.js`/`app.js` 的副本 | 已发生：便携版（2026-09-05 生成）**缺** `pluralBase`（复数拒绝）和 `turnMsLeft`（倒计时修复） | **改前端后必须重跑** `node tools/build_standalone.js` |
| 7 | **规则常量重复** | `aeiouy` 出现在 **4 个文件 6 处**：`logic.js:22`、`analyze_hard_ends.js:23`、`build_unified_db.py:39`、`compute_chain_idx.js:30/40/124` | 改元音集合要改 6 处，极易漏 | 长期：收敛到一处；短期：改前先 grep |
| 8 | **有规则但没测试** | `pluralBase`（复数拒绝）在 `test/` 里出现 **0 次** | 改坏了没人知道 | 新增规则必须配套测试 |
| 9 | **环境陷阱** | ① PowerShell 下 `npm test` 被执行策略挡住（`npm.ps1` 无法加载）② `python3` 是坏的 Microsoft Store 别名 | 照 README 直接敲命令**会失败**，误判为项目坏了 | 用 `npm.cmd test`；用 `python`（不是 `python3`） |
| 10 | **链路描述重复 4 处** | `package.json` build / `build-db.sh` / `README.md` / `DEPLOY.md` | 改一步要同步 4 处，必然漂移 | 以 `package.json` 为唯一真相源，其余只做引用 |
| 11 | **重建词库代价高** | `compute_chain_idx.js` 遍历 33 万词约 **90 秒+**，`db.json` 84MB | 误跑/反复跑浪费时间，且会改动已入库的 `public/vocab.json` | 见第 5 节铁律：先备份、别乱跑 |
| 12 | **房间是内存态** | `src/rooms.js` 用普通对象存 `rooms`/`userRoom` | 服务重启房间全丢，无法多进程部署 | 现状可接受；若要多进程必须先落盘 |
| 13 | **构建期索引建在过滤之前**（本轮新发现，已修复） | `compute_chain_idx.js` 原第 28 行 `var store = new R.WordStore(db)` 用的是**过滤前**的全量库，而过滤在其后 200 行才执行 | `succ_cnt`/`has_succ`/`chain_idx` 把**已被删除的词**也算成后继：**29,770 个词后继数虚高**，**20 个词被错标为"接得上"**（`onyx`/`oryx`/`coccyx`/`archaeopteryx` — 它们唯一的接法是被删掉的 `yx`/`alx`）。AI 会以为某个词还能接下去 | 已改为"**先过滤 → 再建索引 → 再算可接指数**"（把这段包成 `computeSucc()`，在过滤之后调用）。**通用教训：凡是"先算指标、后裁剪数据"的脚本，裁剪后的指标一定是脏的** —— 顺序必须是先裁剪再计算 |

**#13 的排查方法（值得复用）**：不要只读代码，而是**用过滤后的词库重算一遍，和现有字段逐词对比**。
当时的探测脚本只用 20 行就定位了问题——输出"重算后 succ_cnt 变小 = 29,770 / has_succ 由 true 变 false = 20"，
两个数字直接锁定了影响面和危害等级。

---

## 4. 工作流：分阶段推进

**总原则：一次只动一层，每阶段结束时项目必须是"可运行 + 测试全绿"的状态。**

### Phase 0 — 基线固化（先做，约 20 分钟）

**目的**：让后面每一步都有对照，出问题能立刻知道是哪一步引入的。

| 步骤 | 动作 | 注意 | 验收 |
|---|---|---|---|
| 0.1 | 跑全套测试记录基线 | PowerShell 里必须用 **`npm.cmd test`**（`npm test` 会被执行策略挡住） | 7 套全绿，124 项 0 失败 |
| 0.2 | 记录词库基线数字 | 见 1.3 表格；数字变了说明词库被动过 | 282,551 / 36,809 / 28,531 |
| 0.3 | 建工作分支 | `git checkout -b fix/wordbank-quality`，别直接在 main 上改 | `git status` 干净 |
| 0.4 | 处理未提交改动 | 当前有一处未提交：`tools/compute_chain_idx.js`（`[网络]` 过滤）。**先决定它是留、是改、还是撤销**，别带着它开始新工作 | 工作区干净 |

> ⚠️ **0.4 是本阶段最容易踩的坑**：那处未提交改动正是"空转"的那条正则。带着它继续改，后面会分不清哪次改动导致了什么。

### Phase 1 — 低风险修复（收益最大、风险最小，约 1～2 小时）

**目的**：修掉"写了但没生效"的规则，让词库质量立刻提升。

| 步骤 | 动作 | 注意 | 验收 |
|---|---|---|---|
| 1.1 | 修 `[网络]` 正则锚点（`^` 去掉） | 改完**必须实测删了多少**。预期删 283 个；若删 0 个说明还是没匹配上 | 实测删除 283，且抽查被删词确为机翻/黑话 |
| ~~1.2~~ | ~~补专名正则~~ ✅ **已完成（并升级为"限额制度"）** | 见下方"专名限额制度"小节 | 专名漏网 7,056 → **0**；常见词零误伤（apple/water/warehouse/city 全部仍是 kind=0.9） |
| 1.3 | 重建离线单文件 | `node tools/build_standalone.js`；这会覆盖 11MB 的产物文件 | 便携版含 `pluralBase` 与 `turnMsLeft` |
| 1.4 | 提交 | 一个逻辑改动一次提交，中文提交信息写清"改了什么/为什么" | `git log` 清晰可回溯 |

> ⚠️ **Phase 1 的三个致命注意**：
> 1. **改 `compute_chain_idx.js` 后必须重跑整个构建**（`node tools/compute_chain_idx.js`，28 万词约 90 秒），否则改的是脚本、词库还是旧的。
> 2. **重跑只写 `data/db.json` 一个文件**（已实测确认，`public/vocab.json` 不受影响），而 `data/db.json` 被 gitignore 忽略 —— 所以重跑不会污染入库文件。备份建议用 `cp data/db.json data/db.bak.json`（这个名字匹配 `data/*.json`，会被自动忽略，不会弄脏 `git status`）。
> 3. **每次只改一条规则，立刻验证效果**。同时改 3 条，就再也分不清是哪条起了作用（`[网络]` 空转就是这么来的）。

> ✅ **关于"重跑是否有副作用"的实测结论**（2026-09，可用于判断能否放心重跑）：
> 在 28 万词库上重跑 `compute_chain_idx.js`，`chain_idx` 确实会被重算（因为供给口径取决于当前词库内的后继词），但**漂移极小**：63.6% 的词有变化，平均绝对变化仅 **0.0015**，最大 0.0443。
> 结论：**重跑近似幂等，可以放心执行**，不必担心"重跑一次词库就变样了"。

#### 功能 × 模式 范围矩阵（用户逐条确认，改动前必读）

"竞技规则"**只在联机房间启用** —— 单机玩法保持轻松。凡新增会"限制玩家"的功能，先确认它属于哪一格。

| 功能 | 人机对战 | 本地同屏 | 联机房间 | 离线便携版 | 实现处 |
|---|---|---|---|---|---|
| 词库清理（依赖型短词/回声热词已删） | ✅ | ✅ | ✅ | ✅ | `compute_chain_idx.js` 过滤块 |
| **禁止回声** | — | — | **✅** | — | `canChain(prev, word, {strictEcho, echoOk})` |
| **专名限额 3/局** | — | — | **✅** | — | `properQuotaError(pl, entry, strict)` |
| **AI 裁判验词** | — | — | **✅** | — | `createGame(players, {referee:true})`，只有 rooms 传 |
| **道具（买 + 用）** | — | — | **✅** | — | `gameplay.useItem` 检查 `mode === 'room'` |
| **积分（挣分）** | ✅ | — | ✅ | — | `points.ownerOf` 对 `mode === 'local'` 返回 null |

**统一由 `game.mode` 表达**：`'pve'`（人机）/ `'local'`（本地同屏）/ `'room'`（联机房间）；`logic.js` 默认 `'pve'`。
设置处：`api.js` 的 `/api/start`（有 AI → `pve`，全人类 → `local`）、`rooms.js` 的 `startRoom` 与单挑开局（→ `room`）。

**三条容易踩的**：
1. `canChain` 的 `strictEcho` 是**开关**、不是默认行为 —— 不传就是"允许回声"，这正是单机模式要的。
2. `WordStore.candidates()` 也必须传 `strictEcho`，否则房间里的「提示」会推荐一个出词时必被拒的词。
3. **构建期（`compute_chain_idx.js`）不套用回声规则** —— `chain_idx` 是人机/同屏/离线都在用的指标，
   这三个模式允许回声，套用会让可接指数偏悲观。（原先"构建期与运行期两处必须同步"的约束，
   只在规则变回全局时才会重新需要。）

#### 专名限额制度（本项目的新设计约定，改动前必读）

**背景**：ECDICT 里 7,056 个人名/地名/姓氏之所以危害大，是因为它们被当成普通词（`kind=0.9`）——
在 AI 评分里既不被降权，又因"末尾 2 字母匹配"拿到极高可接指数，于是 AI 会把
`aage`（`n. (Aage)人名；(丹)奥格`）这类词主动喂给玩家。

**采用的方案：保留 + 降权 + 限额**（而**不是**从词库删除）：

| 层 | 做法 | 位置 |
|---|---|---|
| 词库 | 专名识别为 `kind=0.10`，**保留在词库中**（删掉会制造死路，玩家反而无词可接） | `tools/compute_chain_idx.js`：`PROPER_RE3` + 过滤条件写成 `kind < 0.1`（**注意是小于号**，写 `<=` 会把专名全删掉） |
| AI | `kind=0.10` 自动触发词型惩罚（`soft × 0.378`），可接指数从 ~0.9 掉到 ~0.1；额度用完后 AI 完全不选专名 | `public/logic.js`：`scorePick` 的 `ctx.noProper` + `tickAI` |
| 玩家 | 每人**每局**最多 `PROPER_QUOTA = 3` 个，超出即拒；**跨轮不重置** | `public/logic.js`：`properQuotaError` + `Game.submitStart/submitChain` |
| 前端 | 记分板显示「专名 N/3」，接龙面板给专名打「专名」徽章 | `public/app.js` + `public/style.css` |

**为什么按"局"而不是按"轮"**：`Game.newRound()` 只清空本轮重复判定，**不动** `players[].properUsed`。
若要改成按"轮"计，在 `newRound()` 里重置该字段即可 —— 但先想清楚：一轮可能只有几个词，按轮计等于几乎不限。

**改动这块时的注意**：
1. `PROPER_QUOTA` 与 `isProperEntry` 在 `logic.js` 里，被**服务端 / 浏览器 / 便携版三方共用**，改完必须重跑 `node tools/build_standalone.js`。
2. 词库侧（`kind`）与游戏侧（限额）是**两套独立机制**，可以分别调整：只想让 AI 少喂专名就只调权重；只想限制玩家用量就只调 `PROPER_QUOTA`。
3. ~~**专名限额不解决** `-lar` 类问题，根因是"2 字母回退"顶满可接指数~~ → **两次纠正后的最终结论**：
   ① 我一开始以为这是"AI 喂生僻词"问题，用 AI 出词占比衡量 → 只有 0.3%，判定为低收益；
   ② 用户纠正：这是**房间对战（人与人）**的问题，跟 AI 无关，**看 AI 指标完全跑偏了**。
   真问题是"回声偷懒"（见下方「禁止回声」小节）。
   **教训：先确认"在哪个模式下痛"，再选指标。用错模式的指标会把真问题判成伪问题。**

#### 词条可信度 conf / 难度猜测标记 dGuess（本项目的新设计约定）

**发现的真问题**：`build_unified_db.py` 的 `_derive_diff` 以 `best = 5` 起步，只凭考试标签/柯林斯星级/词频/词长推导难度。于是：

| 现象 | 数据 |
|---|---|
| `d=5` 的词 | **227,775** 个（占全库 **80.7%**） |
| 其中无任何权威佐证 | 204,963（90%） |
| 无佐证词的难度实际由什么决定 | **只有词长**：≤13 字母→5，>13→**7**（这解释了 `d=7` 的 31,144 尖峰） |

**危害**：AI 的难度贴合 `S2 = 1-|d-target|/10`，对中等玩家（target≈5）而言，这 22.5 万个"难度未知"的词**全部拿满分 S2=1.0**，反而比真正有难度数据的词更占优——**难度匹配变成了奖励"未知"**。

**处理**（`tools/compute_chain_idx.js` 写字段 + `public/logic.js` 用于打分）：

```js
e.dGuess = !authed && !e.has_note;   // 难度仅为词长兜底推断 → 打分时 S2 降为中性 0.5
conf = has_note ? 0.9 : authed ? 0.8 : DOMAIN_TAG ? 0.3 : 0.45;
soft *= (0.60 + 0.40 * conf);        // 缺 conf 字段的旧词库按 0.85 中性处理，行为不变
```

**实测效果**（30 局 ×2，AI 出词 1,500+ 次/组）：

| 指标 | 改前 | 改后 |
|---|---|---|
| **选中的 d=5 词里"难度纯属词长兜底"的占比** | 词库基准 **90.0%** | **9.3%** ← 关键改善（10 倍） |
| 无权威佐证 | 7.5% | 4.0% |
| 含领域标签 | 11.3% | 10.0% |
| 生僻 f<0.45 | 20.4% | 20.4%（**未变**，见下） |
| 带知识点 | 91.9% | 91.0% |

**必须诚实记录的两点**：
1. **生僻词占比没降**。因为那 20% 里多数是 Kyle 精讲的考试词（`ingenuity`/`enshrine`/`ingrained`），它们有 `has_note` 故 `conf=0.9` 不降权。**这属于预期行为**（AI 本就该在玩家水平上沿出题），不是缺陷。
2. **`d=5` 总占比也没降**（33.0%→33.7%），但**成分彻底变了**：从"难度未知的随机词"变成"有柯林斯星级/词频佐证的中等词"（`service`/`near`/`achieve`/`aware`）。所以看总量指标会误判，**要看成分**。

**改动这块时的注意**：
1. `conf`/`dGuess` 由**词库构建**写入，所以改完必须重跑 `node tools/compute_chain_idx.js`（~90 秒）。
2. `db.lite.json`（随仓库分发的轻量库）**没有**这两个字段，走中性默认。若要让便携版也享受该优化，需重建轻量库（已记入待办）。
3. 调权重的入口是 `soft *= (0.60 + 0.40 * conf)` 里的 `0.60`：改成 `0.5` 表示最差词条只保留一半权重；改成 `1.0` 等于关闭该机制。

### Phase 2 — 工具链与产物治理（约 1～2 小时）

**目的**：消灭"会自己腐烂"的东西，这是"免得难以维护"的关键。

| 步骤 | 动作 | 注意 | 验收 |
|---|---|---|---|
| 2.1 | 新建 `tools/check_db.js`：**词库体检脚本** | 一次输出：总词数、kind 分布、专名漏网数、无权威佐证且高可接指数的词数、`-lar` 类可疑结尾数 | 一条命令看清词库健康度 |
| 2.2 | 修 `print_hard_list.js` 字段（`rep.words`→`rep.rows`，`rep.summary.*`→`rep.meta`/`rep.hardCount`） | 改完**必须实际运行一次**（这个坑正是"没运行过"造成的） | 能正常输出难接结尾清单 |
| 2.3 | 重跑 `analyze_hard_ends.js` 刷新过期报告 | 它读 `data/db.json`，需要全量库在 | 报告 meta 的 vocabSize = 282,551 |
| 2.4 | 给 3 个入库生成物加"生成标记" | 在 README 或文件头注明：谁生成、用什么命令、何时生成 | 后续开发者知道不该手改 |

> ⚠️ **注意**：2.1 的体检脚本是**后续所有词库改动的安全网**。先有它，再动 Phase 3；否则 Phase 3 改了什么、好不好，全靠肉眼。

### Phase 3 — 词库质量（核心，工作量最大）

**目的**：解决"AI 把生僻词喂给玩家"的根本问题。

| 步骤 | 动作 | 注意 |
|---|---|---|
| ~~3.1~~ | ~~先量化，再改~~ ✅ **已完成**：模拟对局脚本跑了 30 局 ×2 组、共 3,000+ 次 AI 出词 | **重要纠正**：词库层面"57% 可疑"**并未**转化为游戏伤害——实际 AI 出词只有 7.5% 无权威佐证、91.9% 带知识点。之前的表述夸大了 |
| ~~3.2~~ | ~~处理"2 字母回退顶满可接指数"~~ ⚠️ **已重新定性**：根因不是"结尾"，而是**难度数据全是默认值** | 实测 `d=5` 有 227,775 个词（80.7%），其中 90% 无任何佐证；`_derive_diff` 以 `best=5` 起步，无佐证的词难度**只由词长决定**（≤13→5，>13→7，后者解释 d=7 的 31,144 尖峰）|
| ~~3.3~~ | 结尾维度限制（`-lar` 类） | ✅ **已完成，但问题定性被用户纠正过两次**。⚠️ 我最初用"AI 出词占比仅 0.3%"判定它低收益 —— **这是错的指标**：用户指的是**房间对战（人与人）**，那里根本没有 AI。真问题是"**可以把上词的结尾原样回声回去**"（A 出 `xxxlar`，B 回 `lar`＝词库里的「家庭守护神」，生僻却能接，看起来在偷懒）。已按此实现「禁止回声」规则，见下方小节 |
| ~~3.4~~ | ~~领域专业术语限制~~ ✅ **已完成**（改用统一的"词条可信度"机制，见下） | 73,908 个专业术语条目被判 `conf=0.3` 并降权；AI 出词中领域标签占比 11.3% → 10.0% |
| 3.5 | 每改一轮，跑 `check_db.js` 对比 + 跑模拟对局 | 用数据说话，不用感觉 |

> ⚠️ **Phase 3 三条铁律**：
> 1. **不要在一次提交里同时改"词库过滤"和"可接指数算法"** —— 效果会互相掩盖，无法归因。
> 2. **保留可回退的旧词库**：改动前 `cp data/db.json data/db.bak.json`（84MB，确认磁盘够）。
>    注意备份名必须是 `db.bak.json` 这种**仍以 `.json` 结尾**的形式 —— 写成 `db.json.bak` 不会被 `data/*.json` 忽略，会弄脏 `git status`。
> 3. **验收标准要事先定好**：例如"AI 选出的词中，无权威佐证词占比从 57.3% 降到 X%"。

### Phase 4 — 新功能（按依赖顺序，别并行）

**目的**：做"下个版本"的功能。**顺序很重要，后面的依赖前面的。**

| 步骤 | 功能 | 状态 | 注意 |
|---|---|---|---|
| 4.1 | **密码修改** | ✅ **已实现** | 已落地：`src/auth.js` 的 `changePw`（校验原密码 + scrypt 加盐重算）+ `POST /api/change-password` + 账号栏「修改密码」弹层。**额外安全加固**：用户记录加 `tv`（token 版本号），改密后 +1，使**其他设备上的旧 token 立即失效**；接口同时下发新 token 以保住当前设备登录态。测试见 `test/account-test.js`（新增 10 项） |
| 4.2 | **1v1 单挑**（房间内发起） | ✅ **已实现** | 见下方「1v1 单挑」小节。⚠️ 原需求写的是"拒绝单挑判负"，**用户后来明确要求去掉该机制** —— 实现为"拒绝不打，双方无惩罚" |
| 4.3 | **积分系统** | ✅ **已实现** | 见下方「积分系统」小节 |
| 4.4 | **道具（跳过/反转/修改卡）** | ✅ **已实现** | 三种卡全部落地。反转卡按用户确认的 **(b) 回合逆序** 实现（只倒转出词顺序，接龙匹配规则不变），见下方「道具系统」小节 |

> ⚠️ **Phase 4 的四条通用注意**：
> 1. **每个功能配一个 `test/` 下的测试文件**（现有 7 套测试是这项目的护城河，别让它退化）。
> 2. **先写测试，再写实现**：尤其 4.2 / 4.4 涉及回合规则，靠肉眼测不完。
> 3. **道具必须服务端权威**：客户端的积分/道具状态可被篡改（参考现有安全测试的思路）。
> 4. **4.4 之后必须重建离线单文件**，否则便携版又落后。

#### 禁止回声 + 依赖型短词清理（房间对战体验的核心修复）

**问题**（用户反馈，房间对战）：A 出 `abarticular`，B 直接回 `lar` —— 词库里的 `lar`＝「n. 家庭守护神」，
生僻到没人认识，却能合法接上，**看起来就是在偷懒**。房间对战没有 AI 兜底，谁都能靠这招逃避思考。
**实测规模远大于一个词**：499 个此类短词，合计提供 **6 万+ 次**偷懒机会（`ed` 13,315 / `te` 11,219 / `se` 3,504 最严重）。

**用户最终定下的判据（两句话，是整个设计的核心）**：
1. **高频词允许回声** —— `to` / `be` / `as` 这类常见词回一遍不算偷懒
2. **必须要有独立实际意思才可以保留** —— 若是其他词的**缩写 / 昵称 / 字母名 / 化学符号 / 屈折形式 / 片段**就删去
   （用户原话：「比如 sis 原本是 sister 就删去」）

**实现：两个互补机制**

| 机制 | 作用对象 | 位置 |
|---|---|---|
| **频率门控回声规则** | 仍留在库里的词：高频允许回声、生僻不允许 | `logic.js`：`isEcho` + `canChain(prev, word, {echoOk})` + `WordStore.echoOkFor` |
| **依赖型短词删除** | 直接从词库删掉（212 个） | `compute_chain_idx.js`：过滤块里的 `dependentWhy()` + `echoCount()` |

**「高频/可信」统一口径**（两个机制共用，避免互相打架）：
```js
isTrusted(e) = (e.collins >= 1) || ((e.f || 0) >= ECHO_KEEP_F /* 0.65 */) || !!e.has_note
```

**删除判据**（长度 2-3 且不在白名单且非高频）：
- **A. 依赖型语义**：`等于另一词`(sis)、`字母名`(ess/zed/en)、`化学符号`(te/ne/ag)、`缩写`、`昵称`、
  `屈折形式`(ays/ums/ves =「ay 的第三人称」)、`词素标记`(fer =「pref. 带」)、`缩略语展开`(ac)、
  **无词性标记的片段**(ogy/gy/ory/mic)
- **B. 生僻回声热词**：有 ≥500 个词以它结尾（说明它被实际当作后缀用），如 `lar`(947)/`ier`(938)/`ary`(781)/`ery`(577)

**实测结果**：删 212 个（连同上一轮缩写清理共 379 个）；
**所有有独立意思的真词全部保住** ✓
`oat`/`tin`/`ram`/`hen`/`lid`/`ox`/`log`/`owl`/`ash`/`den`/`mat`/`nil`/`pod`/`fin`/`tar`/`pus`/`ump`/`amp`/`ken`/`mol`/`col`/`ate`/`to`/`be`/`as`/`la`/`ma`
仅 6 个边缘词受损（`um`/`ay`/`os`/`ger`/`fed`/`dal`，输入时仍可用"确认使用"方式出词）。

**实现与必须同步的两处**（⚠️ 本项目唯一的"规则逻辑重复"点）：
1. `public/logic.js` 的 `isEcho` + `canChain` —— 运行时规则（服务端/浏览器/便携版三方共用）
2. `tools/compute_chain_idx.js` 的 `isChainable` —— **构建期快速路径，必须同步**，
   否则构建时会把回声算成"可接"，使 `chain_idx` 虚高、与真实规则不符
   两处的 `ECHO_KEEP_F` 常量也必须一致。

**一致性已被测试锁住**：`test/api-test.js` 断言 `apteryx`（几维鸟，结尾 `-yx`，全库只有 `yx` 能接）
`has_succ === false` —— 同时验证了构建期与运行期规则一致。**以后改接龙规则，务必同时改这两处并跑该测试。**

> ⚠️ **这一个问题上我走了三次弯路，教训比结论更重要**：
> 1. **用错指标**：我最初用"AI 出词占比"衡量，只有 0.3% → 误判为低收益。**房间对战根本没有 AI，这个指标毫无意义。**
>    → **先确认"在哪个模式下痛"，再选指标。**
> 2. **一刀切禁回声**：第一版规则禁掉**所有**回声，连 `to`/`be`/`as` 也一起禁了 —— 用户明确不允许。
>    → **规则要分级，不能一刀切。**
> 3. **想靠频率删词**：试过 `f < 0.65` 一刀切，会删掉 `tin`/`ram`/`hen`/`lid`/`oat`/`ox` 这些**真常用词**
>    （短词既是真词又是后缀：`tin` 有 876 个词以它结尾）。**频率不足以区分"真词"与"片段"。**
>    → 最终靠**语义判据（是否依附于另一个词）**才解决。用户一句"必须要有独立实际意思"直接点破了本质。

#### 缩写清理（词库侧，167 个词；与上面的依赖型清理合计 379 个）

在 `computeKind` 里新增 4 条模式，命中即 `kind=0.05` → 被过滤删除：

```js
if (L <= 6 && /^\s*(symb|abr\.|abbr\.)/i.test(zh)) return 0.05;    // 化学元素符号/缩写前缀
if (/\[\s*=/.test(zh)) return 0.05;                                // [=全称] 等价缩写（physiol/refrig/catscan…）
if (/[A-Z][a-z]{2,}\s*[之的]昵称/.test(zh)) return 0.05;            // 人名昵称（ed = Edwin 之昵称）
if (L <= 6 && /^\s*[A-Z][A-Za-z]+\s*[,，]/.test(zh)) return 0.05;   // 释义以大写英文词+逗号开头
```

实测删除 167 个（`ed`/`se`/`physiol`/`refrig`/`catscan`/`andy`/`kate`/`brrring`…），
**对 `to`/`my`/`no`/`be`/`as`/`by`/`so`/`an`/`net`/`ado`/`owl`/`bunny` 零误伤**。

> ⚠️ **差点酿成大祸的教训**：我最初写的模式是"短词 + 释义含 `[域]`"，实测会删掉
> **`to`/`my`/`no`/`be`/`as`/`by`/`so`/`an`** —— 英语里最常用的虚词！原因是它们的 ECDICT 释义里
> 附带国别码（`no` 也是 Norway、`be` 也是 Belgium）。**收紧为"`[域]` 必须在释义开头"后命中数为 0**。
> 另一处：模式"短词 + 释义含 昵称"会误删 **`bunny`（小兔子）**，因为释义写"儿童对兔子的昵称"；
> 收紧为"**大写英文人名** + 之/的昵称"后安全。
> **凡是按释义做过滤，都必须先跑一遍"最常用词是否中招"的校验，不能只看命中量。**

#### 游戏黑话 / 机场代码 / 文件后缀名（词库侧，11 个词）

第三轮清理。`check_db.js` 的"短词风险面"体检揪出来的：

```js
var GAME_JARGON_RE = /\[(魔兽世界|暗黑破坏神|梦幻西游|传奇|网游|网络游戏|游戏)[^\]]*\]/;
var CODE_WORD_RE   = /机场代码|后缀名|扩展名/;
if (!(e.collins > 0) && (GAME_JARGON_RE.test(e.zh || '') || CODE_WORD_RE.test(e.zh || ''))) return false;
```

删除 11 个：`ony`（[魔兽世界]黑龙公主巢穴）、`yx`/`kuk`/`sheal`/`valk`/`zon`（[暗黑破坏神]黑话）、
`alx`（[魔兽世界]地名）、`cky`（科纳克里机场代码）、`bas`/`ini`/`exe`（文件扩展名）。

> **为什么它们前两轮没被清掉**：`dependentWhy()` 要求释义**没有**词性标记才算"片段"，
> 而这 11 个都带着 `n.`，所以顺利通过；`echoCount` 又都低于 `ECHO_HOT_MIN=500`，第二道闸也没拦住。
> **教训：两条过滤规则的"交集缝隙"会漏词 —— 每加一条规则，都要用体检脚本重看一遍全库。**

**豁免条件为什么用 `collins` 而不用 `frq`**：`exe`(frq=28292) 和 `zon`(frq=37017) 在语料里出现得不算少，
但释义仍然只是"扩展名 / 见 ama"，没有独立意思。若用 `frq>0` 豁免，这两个会漏网。
用 `collins>0` 则正好保住 `bat`(蝙蝠)、`bin`(箱柜)、`tar`(焦油)、`extension`(延长)——
它们的释义里也提到"扩展名"，但它们本身是真词。

**已知代价（有意接受）**：删 `yx` 会让 **20 个词变成死路**，因为 `yx` 是它们唯一的接法
（`onyx` 缟玛瑙 / `oryx` 剑羚 / `coccyx` 尾骨 / `archaeopteryx` 始祖鸟 / `sardonyx` 缠丝玛瑙…）。
全库本来就有 2659 个死路词，多 20 个不影响平衡；换来的是 `onyx → yx` 这种偷懒接法消失。

#### AI 裁判（验词）—— `src/ai.js`

**作用**：玩家出了可疑的词 → 暂停本轮，弹四选一（3 个释义候选 + 「以上都不对」）让他证明自己认识这个词。
**答错扣 1 分，答对不扣；无论对错接龙都继续**（词本身已被接受）。

**作用范围（用户最终确认）**：
- ✅ **只有联机房间启用**（`gameplay.createGame(players, { referee: true })` 才挂裁判）
- ❌ 人机对战、本地同屏、离线便携版都**不弹验词** —— 这同时消掉了"离线版缺裁判"的缺口
- 因此"出词过快"这个信号也只存在于房间（裁判本身只在房间存在）
- 画像：房间里读**玩家自己的账户画像**（玩家名=用户名）；其它模式不涉及

**规则细节（用户确认）**：
- 限 **15 秒**作答（`ai.CFG.VERIFY_TIMEOUT_MS`，环境变量 `VERIFY_TIMEOUT_MS` 可覆盖）
- **验词期间不跑回合计时器**，两者互斥（`clearTurnTimer` 一并清、`armTurnTimer` 检测到 `game.verify` 就提前返回）
- **答错或超时都是扣 1 分，不判负**，然后**由下一位继续接龙** —— 因为词在弹验词前就已经被接受了，
  判负没有道理。实现见 `logic.js` 的 `answerVerify()` 与 `timeoutVerify()`

**触发判据**（`assess`，权重见 `ai.js` 的 `W`）：

| 信号 | 说明 |
|---|---|
| 生僻度 | 分档：可信词条 0.15 / 无佐证且 f<0.30 → **1.0（硬触发）** / f<0.45 → 0.7 / 其它 0.35 |
| 出词过快 | 距上一个词 ≤1.2 秒 → 1.0；≤2.5 秒 → 0.6（`FAST_MS`/`VERY_FAST_MS`）|
| 难度突跳 | 比该玩家本局此前的平均难度高出一大截 |
| 不像你会用 | 结合**历次对局画像**（`profile.skill` 与 `knownSet`）判断是否超出其水平/没见过 |
| 取巧短词 | 又短（≤4）又不可信 |

`score = 0.50·生僻 + 0.30·速度 + 0.05·突跳 + 0.10·不像你会用 + 0.05·取巧`，达到 `TRIGGER`(0.55) 即触发。

> **为什么要"极生僻必验"这条硬规则**：只靠加权时，一个极生僻词**慢慢打**只得到 0.54 分（差 0.01 就不触发）——
> 实测发现的。能打出 `aatitetetanotolysin` 这种词，就该能说出它的意思，跟打字快慢无关。
> 同理**常见词 + 秒回也不触发**（`apple` 不管多快都只有 0.17 分）—— 常用词快手出词很正常。

**架构：钩子注入（关键设计）**
- 引擎 `public/logic.js` 只负责**存题、判分、推进回合**（`game.verify` / `answerVerify` / `game.aiReferee`）
- 「什么算可疑、题目怎么出」全在服务端 `src/ai.js`，通过 `createGame` 时 `ai.attach(game)` 注入
- 好处：**共用引擎不依赖服务端模块**，离线便携版照常运行（不注入钩子即"无裁判"，行为同旧版）
- 三种模式（人机 / 本地同屏 / 联机房间）都经过 `gameplay.createGame`，因此**一套判定全覆盖**

**⚠️ 安全：答案绝不能下发**
`view.verifyForClient()` 只下发 `word` / `options` / `reasons`，**剔除 `answerIdx` 与 `correctZh`** ——
否则玩家在响应体里就能看到答案。作答后才由 `verifyResult` 揭示。已用测试锁住（`api-test.js`）。

**其它**：同一玩家 `COOLDOWN_MS`(60 秒) 内最多被验一次；换轮 `newRound()` 清空未作答的题；
待作答时其它动作一律被拒（"你有一道验词待作答，请先完成它"）；联机房间中只有本人能作答
（`roomAction` 对 verify 单独放行本人）。分值惩罚会**双向结算到账户**（`points.js`）。

#### 积分系统（本项目的新设计约定）

**数值规则集中在 `public/logic.js` 的 `POINTS` 常量**，改数值不用动逻辑：

```js
var POINTS = { perWord: 1, winRound: 10, longChainFrom: 10, longChainBonus: 1 };
// 每成功出 1 词 +1；赢一轮 +10；单轮接龙超过 10 词后每多 1 词再 +1（给该轮赢家）
// 只加分不扣分 —— 目的是正反馈，不做惩罚性设计
```

**数据落在账户文件** `data/sync/<用户>.json`，新增 `points`（非负整数）与 `items`（道具库存对象）。
`loadUserData` 对老账户（只有 `usage/seen/best/games`）自动补默认值并清洗非法项，**老数据可直接升级**。

**结算归属**（`src/points.js` 的 `ownerOf`）—— 这是最容易搞错的地方：

| 场景 | 积分算给谁 |
|---|---|
| 本地同屏且勾了「统计我的出词」 | 只有 `myPlayerIdx` 那一位，结算到对局账户 |
| 联机房间 | 玩家名就是用户名 → 各自结算到自己的账户 |
| 人机对战 | 人类玩家的积分归到该对局绑定的账户（昵称未必是用户名） |
| 游客 / 未登录 | **不结算**（`auth.hasUser` 拦截） |

**幂等**：`game._credited[i]` 记住每个座位"已结算到多少分"，只写增量。所以每次 `/api/action`
后调用 `points.credit(game)` 都安全；实测「首次结算写入 1 分 / 重复调用写入 0 分」。

**踩坑记录**：
1. `Game.concede` 原先**没有"本轮是否已结束"的守卫** → 重复调用会重复给赢家加分（可刷分）。
   已加 `if (this.roundActive === false) return { alreadyEnded: true }`。现有调用方都会紧跟
   `newRound()`，所以实际不可达，但这类"靠调用方守规矩"的隐式契约很脆，值得堵死。
2. 测试里造用户名要注意：`validUsername` 限制 **2-20 字符**。`'test_pts_' + Date.now()` 是 22 字符
   → 注册被拒 → 后续 5 项测试连环失败。**报错不明显的测试失败，先怀疑前置步骤没成功。**

#### 道具系统（三种卡全部已实现）

**价格与限额集中在 `public/logic.js` 的 `ITEMS` / `ITEM_QUOTA`：**

```js
var ITEMS = {
  skip:    { name:'跳过卡', price:30, available:true },   // 跳过本次接龙（不算认输）
  swap:    { name:'修改卡', price:50, available:true },   // 待接词换成可接指数更高的合法后继
  reverse: { name:'反转卡', price:40, available:true }    // 回合逆序：只倒转出词顺序
};
var ITEM_QUOTA = 3;   // 每局每人可使用道具的总次数（三种卡合计）
```

**反转卡 = (b) 回合逆序**（用户确认的语义）：只倒转"谁先出词"，**接龙匹配规则完全不变**。
实现点极小 —— `Game.startNextTurn()` 依据 `reverseTurn` 决定推进方向（`+1` 或 `-1`），
`useItem('reverse')` = 切换方向 + 按新方向推进（从而跳过使用者自己）。
**未触碰 `canChain`**，所以可接指数、AI 选词、提示功能全部不受影响，AI 也无需改动即自动适应。
效果跨轮保留（本局内持续），再用一次可恢复正序。

`available:false` 的道具**不出现在商店**（`shopCatalog()` 过滤）也不会被 `useItem` 接受 ——
用于"尚未实现/语义未定"的道具，避免卖出一个坏道具。

**服务端权威**（这是道具系统唯一正确的做法）：库存与积分的扣减**只在服务端**发生
（`src/gameplay.js` 的 `useItem`）：
```
校验回合 → 校验登录账户 → 校验库存 → 交给引擎执行 → 成功才扣库存
```
引擎（`logic.js` 的 `Game.useItem`）只管**对局内效果与每局限额**。客户端改不了自己的道具数量。

**归属账户**：`game.user`（人机对战绑定的账户）优先，否则用当前玩家名（联机房间的玩家名就是用户名）。

**两个容易忽略的细节（都已处理并有测试）**：
1. **修改卡生成的是"道具代打的词"，不是玩家出的词**：
   - 不加积分、不更新 `lastWordOwner`（否则认输时会给错人加分）
   - 日志条目带 `byItem:'swap'`，`countWords()` 会跳过它 —— 否则玩家能用道具把接龙长度刷长来骗长接龙奖励
2. **失败不消耗道具**：修改卡找不到"更好接的词"时返回错误且不增加 `itemsUsed`、不扣库存。

**待确认**：~~反转卡~~ 已按用户确认实现（见上）。

#### 1v1 单挑（房间内发起，无判负）

**流程**：房间成员 A 向 B 发起单挑 → B 接受/拒绝。
⚠️ **原需求写的是"拒绝单挑判负"，用户后来明确要求去掉** —— 现实现为**拒绝不打，双方无任何惩罚**。

| 环节 | 规则 |
|---|---|
| 发起条件 | 房间处于 `waiting`；发起者与被邀请者都在房内；不能单挑自己；同时只能有一个待应答邀请 |
| 邀请有效期 | `CHALLENGE_TTL_MS`（默认 60 秒，环境变量可覆盖）；过期自动作废，避免"邀请挂着不动"把房间卡死 |
| 发起者可撤回 | `respond` 传 `accept:false` 且请求者是发起者 → 视为撤回 |
| 接受 | 房间进入 `duel` 状态，创建 1v1 对局（**发起者先手**给开局词） |
| 其他成员 | 等待（可旁观），**不能出词**（`roomAction` 的"轮到你了吗"守卫天然拦住） |
| 结束条件 | 有人认输 / 超时未出词 / 单挑者离开房间 → 对手获胜，房间回到 `waiting`，结果记入 `r.lastDuel` |
| 房主终止 | 房主可终止单挑（无胜负判定，直接取消） |

**实现要点**：让 `r.game` 指向单挑对局，于是**回合计时、快照、轮询同步、道具**等现有机制**全部复用**，
无需为新状态写第二套逻辑。`roomState` 只需把 `status === 'duel'` 也纳入"返回 game 快照"的条件。

**踩坑**：给房间测试新增单挑用例后，**原有的 `离开后 room/mine 为 null` 检查挂了** ——
因为新用例让 A 又进了一个新房间。教训：**新增用例要负责清理自己创建的状态**，
否则会破坏后续用例的隐含前提。已在用例末尾补 `dissolve`。

#### 复核发现（一次"重新审核"专门用来找自己埋的坑）

共 5 处，**没有一处是"功能没做完"，全是做完之后机制互相打架**。下表是**经用户逐条确认后的最终口径**：

| # | 问题 | 最终口径（用户确认） | 落地 |
|---|---|---|---|
| 1 | 修改卡代打的词与成就/记录的关系 | **甲算、乙不算**：计入**接龙长度**；但**不算"你出过的词"**（不进记录/生词表） | `countWords()` 计入它；`chainLen` 用它；该条目带 `byItem:'swap'`，前端 `playedWords()` 据此排除 |
| 2 | 验词与回合计时器 | 验词**独立 15 秒**；期间**停掉回合计时器**；**超时只扣 1 分、不判负**（词早已被接受），**由下一位继续接龙** | 两种计时器互斥（`clearTurnTimer` 一并清、`armTurnTimer` 见 `game.verify` 提前返回）；`logic.timeoutVerify()` |
| 3 | "出词过快"的时间基准 | 修好即可；**该信号本就只存在于联机房间** | 基准改为"上一次 `doAction` 返回时刻"（`_lastActionAt`） |
| 4 | 历次对局画像的适用范围 | **只有联机要**，本地同屏不要 | 裁判只在房间启用；`ai.profileFor()` 房间读玩家自己的账户画像 |
| 5 | 验词作答失败时按钮不恢复 → 弹窗无出口会卡死 | 保留修复 | 出错/网络异常时恢复按钮可点 |

另外：**AI 裁判的作用范围最终收窄为"只有联机房间"**（见下方小节）——顺带消掉了原先"离线便携版缺裁判"的缺口。

> **复核的价值**：这 5 处说明**每加一个新机制，都要回头检查它与旧机制的交互**。
> 本项目最容易出问题的不是"功能有没有实现"，而是**计时器 / 统计口径 / 数据归属**这三类交叉点。

**已知缺口（尚未处理）**：
- **轻量词库 `data/db.lite.json` 已过期，且没有生成脚本**（用户 2026-09 决定：暂不处理，自己只用全量库）。
  实测状态：36,809 词，其中 3,229 词**全量库早已淘汰**（`sis`/`te`/`ide`/`ole`/`mic`/`os` 等依附型/缩写词仍在）；
  `kind` 用的是**修复前**的旧分类（`kind=0.05` 有 646 个，全量库只有 10 个；
  `kind=0.1` 有 980 个，全量库有 7,046 个），且 `action`/`abroad`/`ace` 等超常用词**被误判为专名** ——
  在联机房间里会占用每人每局 3 个的专名额度。字段还缺 `conf`/`dGuess`（`logic.js` 按中性 0.85 兜底）。
  **影响范围**：只有"没跑过 `npm run build` 的全新克隆"和**离线便携版**（`build_standalone.js` 内嵌的就是它）。
  本机存在 `data/db.json` 时游戏用的是已清理干净的全量库，不受影响。
  **若将来要补**：建议写 `tools/build_lite_db.js`，按"现有词表 ∩ 当前全量库"取词（→ 约 33,580 词），
  再用同一套 `computeSucc()` 逻辑**针对精简词表重算可接指数**（不能直接抄全量库的值 ——
  全量库里 `water` 有 1070 个后继，精简库里只有 99 个）。

**已完成（原 Phase 2 遗留）**：
- `print_hard_list.js` 已修复 —— 它读的是 `rows`/`meta` 结构（旧版假设的 `words`/`summary` 早已不存在，这正是它崩溃的原因）。
- `check_db.js` 已建成 —— 一条命令给出词库体检（字段完整性 / 数据卫生 / 核心不变量 / 清理效果 / 回声现状 / 短词风险面）。
  它测出的**短词风险面**口径值得记住：判断"某个短词会不会变成 `lar` 那种偷懒词"，看的不是它在不在库里，
  而是**有多少常见词能把它接出来**。当前 583 个非高频短词里只有 171 个接得上常见词，其中 9 个释义根本不是词。

---

## 5. 铁律（每条都对应一个已发生的坑）

### 改代码前

1. **先跑基线**：`npm.cmd test` 必须全绿才开始改。
2. **一次只改一层**：词库过滤 / 选词算法 / 回合规则，**不要混在一次提交里**。
3. **grep 常量再改**：`aeiouy`、`ry|ht|ck`、`kind` 这类语义散落多处，改前先搜。

### 改代码时

4. **改 `public/logic.js`（共用引擎）→ 必须跑三边**：
   ```
   node test/test.js          # 规则单元
   node test/integration.js   # 真实词库
   node test/api-test.js      # 服务端
   node tools/build_standalone.js   # 重建便携版
   ```
5. **改 `tools/compute_chain_idx.js` → 必须重跑构建并实测效果**："写了规则"≠"规则生效"（`[网络]` 空转就是教训）。
6. **改过滤规则 → 必须双向验证**：既验证"该删的删了"，也验证"**不该删的没被删**"（常见词白名单抽查）。

### 改完代码后

7. **跑全套测试**：`npm.cmd test`（PowerShell 下 `npm test` 会被执行策略挡住）。
8. **同步 4 处描述**：如果改了构建步骤，`package.json` / `build-db.sh` / `README.md` / `DEPLOY.md` 都要看一遍。
9. **不要手改生成物**：`data/db.json`、`data/db.lite.json`、`public/vocab.json`、`hard_ends_report.json`、`word-chain-standalone.html` —— 改它们要通过生成脚本。

### 环境

10. **PowerShell 里用 `npm.cmd`**，不要用 `npm`（`npm.ps1` 被执行策略挡住）。
11. **Python 用 `python`**，不要用 `python3`（那是 Microsoft Store 的坏别名，会报"未找到"）。
12. **重建词库前先备份**：`node tools/compute_chain_idx.js` 会**覆盖** `data/db.json`，且耗时 90 秒+。
13. **别把 stderr 混进管道再判退出码**：`npm.cmd test 2>&1 | Select-String ...` 会把 Node 的
    `DEP0169` 弃用警告当成错误记录，导致外部看到的退出码是 **1**（实际测试全绿）。
    判断通过与否请用 **`npm.cmd test > $null 2>&1; $LASTEXITCODE`** —— 本项目的弃用警告不影响结果。

### 绝不静默失败（这条是踩坑换来的）

14. **任何"提交/执行"类调用的返回值都必须检查**。历史上 `tickAI` 里写了 `this.submitStart(choice.w);`
    却不看 `res.ok`，同时 `interesting` 词表没和 `startable` 求交集 —— 于是 AI 有 **10.3%** 概率
    挑到非法的开局词（`advisory`/`angry` 是 `ry` 结尾），开局静默失败、AI 卡在当前回合。
    表现是**测试随机失败**（10 次里挂 1 次），极难定位。
    修法：① 让 `interesting ⊆ startable`（根因）；② `submitStart/submitChain` 失败时兜底或认输，绝不忽略。

---

## 6. 验收关卡（每个阶段结束必须通过）

```powershell
cd "E:\WorkFolder\DSH Desktop\word-chain"

# 关卡 1：全套测试（必过）
npm.cmd test

# 关卡 2：服务端真能起来 + 接口正常
$env:PORT=8099; node server.js    # 另开窗口，看启动横幅是否打印词库来源
# 访问 http://localhost:8099 与 /api/lookup?word=apple

# 关卡 3：便携版能双击打开（改了前端时）

# 关卡 4：词库体检（Phase 2 之后）
node tools/check_db.js

# 关卡 5：git 状态干净、提交信息可读
git status
git log --oneline -5
```

---

## 7. 自查清单：项目开始"腐烂"的征兆

隔一段时间对照一次，中 3 条以上就该停下来治理：

- [ ] 有生成物被手改过（`git diff` 里出现 `data/` 或 `vocab.json` 的改动）
- [ ] 有工具脚本**跑不起来**了（字段名对不上、路径变了）
- [ ] 有规则**写了但没生效**（正则匹配不到数据）
- [ ] 入库的生成物**明显过期**（时间戳/规模与当前不符）
- [ ] 便携版**落后于源码**
- [ ] 新加的规则**没有配套测试**
- [ ] 同一常量/语义在 **3 处以上**重复定义
- [ ] README/DEPLOY/package.json 对同一件事的**描述不一致**
- [ ] 提交信息只写"优化""修复"，看不出改了什么

---

## 附：当前待办总览（承接第 3 节易碎点）

| 优先级 | 事项 | 归属阶段 | 预估 |
|---|---|---|---|
| ~~高~~ | ~~修 `[网络]` 正则锚点（空转）~~ ✅ **已完成**（删 283 词，0 误伤） | Phase 1 | — |
| 中 | `[网络]` 规则**精度**问题：该规则连带删掉了个别"生僻但真实"的词（如 `niqab` 尼卡布、`sawm` 斋戒）。更优做法是**降权**（把这些词的 `kind` 降到 0.1，让 AI 避开而不从词库删除），需要先定策略 | Phase 3 | 需先定策略 |
| 高 | 修专名正则（7,418 词漏网） | Phase 1 | 30 分钟 |
| ~~高~~ | ~~重建离线单文件（补齐 2 个修复）~~ ✅ **已完成**（随密码修改功能一起重建，已含 `pluralBase`+`turnMsLeft`） | Phase 1 | — |
| 中 | 新建 `check_db.js` 词库体检 | Phase 2 | 1 小时 |
| 中 | 修 `print_hard_list.js` + 刷新过期报告 | Phase 2 | 30 分钟 |
| 中 | 前端改完自动重建便携版（防再次落后） | Phase 2 | 20 分钟 |
| 中 | 补 `pluralBase` 的测试 | Phase 2 | 20 分钟 |
| 低 | 收敛 `aeiouy` 等重复常量 | Phase 2 | 1 小时 |
| ~~高~~ | ~~模拟对局量化"生僻词被喂给玩家"的比例~~ ✅ **已完成**（结论：问题比词库统计显示的小得多） | Phase 3 | — |
| ~~高~~ | ~~处理 2 字母回退导致的 `chain_idx` 虚高~~ ✅ **已完成并重新定性**：实为"难度全默认值"，已用 `dGuess` 修正 | Phase 3 | — |
| ~~中~~ | ~~领域专业术语（`[医]`/`[化]`）限制~~ ✅ **已完成**（统一为 `conf` 可信度机制） | Phase 3 | — |
| 中 | 用 4 个字生成词库时把 `conf`/`dGuess` 也写进 `db.lite.json`（当前轻量库缺这两个字段，走中性默认） | Phase 3 | 需重建轻量库 |
| ~~—~~ | ~~密码修改~~ ✅ **已完成** | Phase 4 | — |
| — | 1v1 单挑 + 拒绝判负 | Phase 4 | 需先定规则 |
| ~~—~~ | ~~积分系统~~ ✅ **已完成**（架构 + 计分 + 结算 + UI + 测试） | Phase 4 | — |
| ~~—~~ | ~~道具系统~~ ✅ **基建 + 跳过卡 + 修改卡 已完成**（商店/库存/每局限额/服务端权威/UI/测试） | Phase 4 | — |
| ~~—~~ | ~~1v1 单挑~~ ✅ **已完成**（房间内发起；按用户要求**无拒绝判负**） | Phase 4 | — |
| ~~—~~ | ~~反转卡~~ ✅ **已完成**（按用户确认的 (b) 回合逆序） | Phase 4 | — |
| — | 道具系统（跳过/反转/修改卡） | Phase 4 | 需先定回合语义 |
